services:
  - type: web
    name: meridian-intelligence-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd services/api && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        value: postgresql://postgres.nrvinsjtkmqkcqztkfam:YOUR_SUPABASE_PASSWORD@aws-0-us-east-2.pooler.supabase.com:6543/postgres

